============
Installation
============

At the command line::

    $ easy_install django-highcharts-models

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-highcharts-models
    $ pip install django-highcharts-models
